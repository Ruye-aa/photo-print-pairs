---
name: photo-print-pairs
description: 将照片转为拼贴、拓印、插画或材质艺术效果，保留原照片上下对照。支持基础与扩展风格选择、参考采集、逐风格审美评价、定向修订、批量预览及精确拼接。
---

# 照片艺术化与上下拼接

保留照片的主要内容，用参考图的形状概括、留白和印刷材质重新表现。不能把加纸纹、调色或照片边缘消散当作完整的风格复现。“像素蛋糕”是参考文件夹名称，不表示像素画，也不能据此认定制作软件。

## 输入与默认值

- 输入一张照片或一个照片文件夹；用户指定的参考图、风格和输出路径优先。
- 风格未指定时，逐图选 A（撕纸拼贴 / 干刷版画）或 B（颗粒拓印 / 流动消散）；默认每张一种。用户要求清爽抽象、印刷海报或轻透插画时，分别选择 C、D、E，见[风格选择](references/style-guide.md)。只有用户要求多版时才生成多种风格。
- 蜡笔、轮廓涂鸦、黏土、摄影拼贴、动漫、City Pop、针织毛绒、积木、像素拉伸，以及邮票版式、胶片调色，使用[扩展目录](references/extended-styles.md)与 `scripts/prepare_extended.py`。11 条路线包含 9 条艺术化、1 种版式和 1 种调色，类别不得混称；未指定时不自动取代 A/B 默认选择。
- 默认无文字、无新增实物或装饰圆点，保留主要对象数量、方向、相对位置和遮挡。可删去的次要背景在本张提示词中明确写出。
- 默认成品是一个 PNG：上方完整原照片，下方效果图。原图 W×H → 成品 W×2H，原照片不裁切、不缩放；效果图等比适配。
- 用户说“与参考图尺寸相同”时，读取所选参考的实际宽高，不能一律假定为 1080×1620。固定尺寸默认等比留边，并说明上方照片经过缩放。奇数总高度由下半部多占 1 像素。
- 用户说“图片/生成”且未指定其他目录时，默认使用当前用户的 ~/Pictures/生成。后续新批次可放入该目录的日期子目录；用户要求直接放入时照做。已有同名文件保留，用版本名存新结果。

## 选择工作范围

已有成品，只要求保存或改位置：核对对应关系、尺寸和文件完整性后复制，不重新生成、不把上下两部分拆成两个文件。

新照片需要艺术化：完成以下流程。缺少内容照片时先定位用户给出的路径；不要拿风格参考中的照片替代用户输入。

### 查看输入与参考

对所有待处理照片确认能解码、方向和尺寸，逐张查看内容。可以先看带编号总览，但小物体计数、模糊或截断处应查看完整照片。区分清晰可见对象和无法可靠计数的细节。

内置的 13 张参考在 assets/reference-originals/。按[参考目录](references/style-catalog.md)选择题材，再用 view_image 查看实际文件。参考是完整上下对照图：新照片负责内容，参考仅下方艺术区负责风格；中线和原照片裁切比例不能仅凭文件高度推断。尺寸及来源记录在 [参考清单](references/reference-manifest.json)。用户给新参考时重新分析，不套用旧目录的结论。

### 编写并执行生成提示词

先写简短内容描述：主要对象、必须保留的数量与空间关系、允许合并或省略的细节、配色和构图。读所选风格的[规则](references/style-guide.md)和[提示词模板](references/prompting.md)。A 类说明合并哪些形状；B 类说明哪些轮廓清晰、颗粒朝哪里变稀；C 类采用无纹理的平面概括；D 类统一有限印色与网点；E 类保留形体，以轻透色层重新描绘。不同材质规则不混用。

用 `scripts/plan_artwork.py` 将已查看照片后的描述编译为计划，读取输出计划文件中的完整 `prompt` 交给图片生成工具，见[计划与修订命令](references/workflow.md)。脚本只校验字段、绑定源图、组织风格规则，不会识别照片内容，也不会调用生图服务。简单单图由助手填写描述，用户无需手填 JSON。计划只代表准备完成，不代表已生成或通过视觉评价。

扩展路线先运行 `python3 <skill目录>/scripts/prepare_extended.py --list`，读取所选路线的实际参考及 [extended-styles.json](references/extended-styles.json)，再编译独立计划：

    python3 <skill目录>/scripts/prepare_extended.py \
      --photo /绝对路径/照片.jpg --style crayon-vignette \
      --subject "主体和场景简述" \
      --preserve "必须保留的细节" \
      --simplify "本次允许简化的细节" \
      --output /绝对路径/生成/扩展计划.json

`--preserve`、`--simplify` 可重复。描述可由助手查看照片后填写，不把助手拟定的细节伪称为用户逐项批准。计划绑定方向校正后的宽高与源文件哈希，保存完整提示词、来源和待审状态；同名不同内容拒绝覆盖，另存新版本。阅读全部 `prompt` 后用内置图片工具执行，计划本身不调用生成。

超现实摄影拼贴和像素拉伸需要用户已允许相应构图编辑，再传 `--allow-recompose`；已有会话授权有效，不重复索要。前者仅允许原有主体重排和简单纸色背景重置；后者仅允许一条抽象彩带，原有主体位置与场景几何继续保留。不得新增人物、实物或替换为别的场景，不能因为该参数存在就给其他路线额外权限。扩展规则中的 `deferred` 不是已适配路线。

需要成组输出时，可复用 `--series` 中的风格、概括程度、配色和纸底；每张的主体与空间关系重新观察，不复制上一张对象。只有用户明确选择沿用或保存偏好时才建立系列配置。不将一次助手的候选选择写成用户偏好。

使用当前环境的内置 image_gen；相关能力可用时遵循 imagegen skill。生成前查看所有本地输入。每张照片的每个目标风格单独调用，明确输入图的角色；只生成独立艺术图，之后再与原照片拼接。按工具当前提供的参考图参数调用，不猜测模型、种子、费用、尺寸参数或保存路径，也不因批量任务而切换到 API。工具不可用时保留准备工作并说明限制；CLI/API 路线需要用户明确选择。

生成的艺术区尽量与原照片宽高比一致，避免历史试验中“一律生成竖图”带来的大片补白。工具只支持近似画幅时，保留比例补纸底。若使用参考图，计划中记录的顺序必须与传给工具的顺序一致；第一张始终为内容照片。保存实际返回的成品、最终提示词、参考文件、版本关系；不要只保存临时生成路径。

提示词中的精确分辨率只是生成请求。打开实际返回文件核对尺寸，最后用本地拼接落实所需尺寸；不得因提示词写了尺寸就声称成品已符合。

### 评价与修订

逐张对照输入、参考和输出，分别判断：主体保留、风格接近、留白与构图、所选材质、额外文字或水印、是否出现新增对象。C 的纸底应平整无颗粒，不能因没有纸纹判失败。清楚记录省略、变形和过量细节，不把“好看”替代保真。数量看不清时写出不确定处，不猜测数量或声称全部保留。

每种新风格适配后，必须实际查看参考、输入和生成结果，分别记录三类证据：

- 参考一致性：引用已查看的来源、页码或实际视频 PTS，说明材质、线条、布局、留白等成立或偏离之处。只读提示词或参考文字不算完成对比。
- 主体保留：以这张输入和计划为依据，核对主要对象数量、身份特征、方向、位置、遮挡和被允许省略的内容，明确剩余问题。
- 审美：分别评价构图层次、配色、质感和完整度，给出可定位的画面依据。可用 0–5 分辅助展示，但必须写评分锚点，标明是助手的主观视觉判断，不能当成客观模型分或人类专家验收。

三类评价各自成立，不能用配色好看抵消主体缺失，也不能用主体保留替代风格一致性。每个结果关联输入、输出哈希、所用计划和参考。保留失败图、原评价、修订理由及最后选择，不在新版出现后抹除旧版失败记录。单张候选通过只说明该输入和该版本的表现；未做跨题材、重复生成验证时，不声称风格稳定。公开仓库中的试生成与修订可查 `examples/research-trials/README.md`；安装后的 skill 不复制 examples，需要时从仓库查看。

参考[历史验证与失败案例](references/validation-notes.md)；需要具体内容约束时检索 [22 组历史提示词](references/prompt-examples.json)。这些是候选结果和历史尝试，不是用户批准的风格标准或成功率依据。

针对具体失败修订，默认每图最多额外修订 2 次，仍有问题就保留候选并报告。发现参考文字被带入时强化禁字；一次修订仍残留，可只用内容照片与文字风格描述重新生成，但应重新检查整个构图。用户只要求保存已有图片时不启动修订。

把“太写实”“花少了”“留白多一些”等反馈转为具体问题、修改说明和需要保留的部分，用 `--feedback` 或预览导出的 `--review` 编译新版本计划。针对已有结果编辑时，先查看被选中的艺术图，并记录实际传入的编辑目标；工具不支持该方式时使用源照片重新生成。改动后检查全图，不能承诺模型只改变指定部分。旧结果与旧计划保留。预览里的“保留”是用户选择，不会把历史助手评价自动升级为用户验收。

### 拼接与验证

用户要求精确上下拼接时，用 scripts/compose_pair.py 做确定性排版。它只处理尺寸、留边和拼接，不能代替生成工具制作艺术效果。依赖 Python 3 与 Pillow；优先使用已有运行环境。

调用示例：

    python3 <skill目录>/scripts/compose_pair.py \
      --photo /绝对路径/原照片.jpg \
      --artwork /绝对路径/选定效果.png \
      --output /绝对路径/生成/照片名_A.png

默认保留整个艺术图，以暖白留边。纸底色不同可用 --paper-color '#EEE8DA' 指定。只有视觉确认是可缩短的纸面空白时才传 --art-crop LEFT,TOP,RIGHT,BOTTOM，记录裁切范围，不能自动截掉主体。可用 --paper-sample 指定经查看确认的纯纸底矩形作为补边纹理，不要取到字或主体。

固定参考尺寸增加 --size 1080x1620（填实际尺寸）。脚本验证成品尺寸、上方照片的实际像素或缩放后像素，在 .records/ 保存尺寸、哈希、裁切和适配方式。同名不同内容自动递增版本；同样内容重跑复用原文件。

增加 `--plan /路径/plan.json` 将生成计划绑定到拼接记录；源照片哈希或方向校正后尺寸不一致时停止。用户需要单独效果图时增加 `--export-artwork`，另存与成品下半区相同的 PNG，默认仍只输出上下对照图。

`--plan` 当前只接受 `plan_artwork.py` 的 A–E 计划。`prepare_extended.py` 生成的扩展计划不能传入该参数；按普通输入、艺术图和输出路径拼接，并把扩展计划与结果一同保存。不得把未绑定的扩展计划记录说成已经通过 A–E 的计划校验。

批次完成或需要用户挑选时，用 `scripts/review_gallery.py --records /结果目录/.records --output /结果目录/review.html` 生成离线预览。页面可标记保留、修改、放弃并导出 `review.json`。导出的修改意见按输出哈希匹配具体版本，见[工作流说明](references/workflow.md)。工程检查和用户评价分开显示，图片不能因文件检查通过而自动视为满意成品。

批量检查文件数、原照片到效果的对应关系，查看所有拼接图或足够清晰的分页总览。不能用一张总览代替独立 PNG。最后给出保存位置、张数、尺寸策略、所用生成模式与提示词记录，以及具体效果结论。只有上方像素检查通过才能声称原照片像素保留；缩放、颜色转换或导出放大应准确说明，放大尺寸不表示新增真实细节。

## 网上照片验证（仅在用户要求时）

覆盖风景和日常随拍，包括简单主体与复杂背景。联网检索可下载和改编的照片；保存页面链接、作者、许可依据、下载链接、下载时间、尺寸和哈希，无法核验时不猜许可。

下载后逐张看真实内容再命名，不能仅凭网页标题判断物体数量。固定输入集后记录所有结果和修订，先定义数量、方向等约束，再评价；没有重复试验和独立评审时，不声称稳定还原或给出客观成功率。规模按用户要求；未规定时选择覆盖题材的小批次，不自动继承历史的 16 张或 22 张规模。

## 网上风格参考采集（仅在用户要求时）

优先采集可正常访问的笔记、图片和视频，记录检索词、筛选方式、实际访问范围及失败项。把登录限制、内容下架和搜索覆盖边界写清楚，不能把搜索结果说成平台全部内容；不绕过访问控制。实际采集记录示例见[采集说明](references/research/2026-09-06/collection.md)。

图片优先保存原文件。下载失败但浏览器可以正常显示时，使用当前浏览器的图片区域截图保存在本地；不要把头像、评论、二维码或整个页面当成照片。截图应保留图中原有署名或水印，不把它标为原始下载。记录 `source_id`、规范来源 URL、页码、实际尺寸、SHA-256、`capture_method=screenshot` 和下载失败原因；原图下载使用 `capture_method=original_download`。不能访问或截图也失败时，保留缺失记录，不编造内容。

视频能够下载时保存原视频并另做可播放性核验。只能取得关键帧时，使用 `capture_method=video_frame`，记录实际帧 PTS、时间基准及对应来源，不用请求的抽帧时刻冒充实际解码时间。几个关键帧不能视为完整视频，静态帧评审也不能证明运动连续性或视频生成效果。只有截图证据、无法可靠读取 PTS 时明确标注时间未知，不猜数值。

已保存的媒体可用 `scripts/research_media.py` 导入、核对和生成公开来源清单；它不负责浏览、下载或截图。第三方原图、视频、截图保存在本地 `work/`，不随公开 Git 仓库上传。公开内容保留来源、页码或 PTS、哈希、采集方式和分析，去除访问令牌、登录信息及本机路径；截图不自动改变原素材的复用许可。新路线仅列入目录不算适配完成，必须完成上文的真实生成和逐风格三类评价。
